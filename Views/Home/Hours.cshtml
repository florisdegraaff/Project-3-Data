@{
	var teachers = (Dictionary<string, int>)ViewData["Teachers"];
}

<h2>@ViewData["Title"]</h2>

<table class="table" id="teachers">
	@{
		foreach (var teacher in teachers)
			{
				<tr>
					<td>@teacher.Key</td>
					<td>@teacher.Value</td>
				</tr>
			}
	}
</table>

<script type="text/javascript">
 
	window.onload = function () {

		var table = Document.getElementById("teachers");
		

		for (var r = 0, row; row = table.rows[r]; r++) {
			var tempRowArray;
			for (var c = 0, col; col = row.cells[c]; c++) {
				var tempColArray;
				temp
			}  
		}
		var chart = new CanvasJS.Chart("chartContainer", {
			theme: "theme2",
			animationEnabled: true,
			height: 400,
			width: 600,
			data: [
				{
					type: "area", //change type to bar, line, area, pie, etc
					dataPoints: [
						{ x: 10, y: 71 },
						{ x: 20, y: 55 },
						{ x: 30, y: 50 },
						{ x: 40, y: 65 },
						{ x: 50, y: 95 },
						{ x: 60, y: 68 },
						{ x: 70, y: 28 },
						{ x: 80, y: 34 },
						{ x: 90, y: 14 }
					]
				}
			]
		});
		chart.render();
	};
</script>

<div id="chartContainer"></div>